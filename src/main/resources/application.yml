server:
  port: 8282

# Multipart Configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
      file-size-threshold: 200KB

    context-path: /api/v1

spring:
  profiles:
    active: dev

